# --- Core runtime: the DEFAULT experience (Landmark MLP + Supertonic TTS) ---
# This set is light enough to deploy on Streamlit Community Cloud's free tier.
# mediapipe pulls in opencv-contrib-python (provides cv2); packages.txt adds the
# matching system libs (libgl1, libglib2.0-0).
numpy>=1.24
pandas>=2.0
mediapipe>=0.10
scikit-learn>=1.3
joblib>=1.3
pillow>=10.0
streamlit>=1.30
streamlit-webrtc>=0.47
av>=10.0

# Text-to-speech: Supertonic (Hugging Face, ONNX, on-device)
supertonic>=1.2
soundfile>=0.12

# For the heavier optional recognizers (Hugging Face SigLIP2, the CNN), the
# notebook, dataset download and local audio playback, also install:
#   pip install -r requirements-extras.txt
