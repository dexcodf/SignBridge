# Core data / numerics
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3

# Computer vision
opencv-python>=4.8
mediapipe>=0.10

# Deep learning (gesture CNN)
tensorflow>=2.13

# Hugging Face real-photo ASL recognizer (better live-webcam accuracy)
transformers>=4.45        # SigLIP2 support
torch>=2.2
torchvision>=0.17         # image processors for the vision pipeline
pillow>=10.0

# Dashboard
streamlit>=1.30
streamlit-webrtc>=0.47   # live webcam streaming in the Recognize tab
av>=10.0

# Text-to-speech: Supertonic (Hugging Face, ONNX, on-device)
supertonic>=1.2
# Fallback TTS + audio playback
pyttsx3>=2.90
sounddevice>=0.4
soundfile>=0.12

# Dataset download
kaggle>=1.6

# Notebook — ipykernel is enough for VS Code / `jupyter` UIs and avoids the
# deeply-nested JupyterLab labextension files that break Windows' 260-char
# path limit. Want full JupyterLab? Enable long paths first (see README).
ipykernel>=6.0
nbformat>=5.0
